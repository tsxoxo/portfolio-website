---
import { t } from 'i18next'

interface Props {
	description: string
	skill_tags: string[]
	tech_tags: string[]
	repoURL: string
}

const { description, skill_tags, tech_tags, repoURL } = Astro.props
---

<div
	class="description-overlay invisible absolute z-10 flex h-full w-full flex-col justify-between bg-white opacity-0 transition-all duration-300"
>
	<div class="flex flex-col gap-2">
		<div class="flex gap-2">
			{skill_tags.map((skill) => <span>{skill}</span>)}
		</div>
		<div class="flex gap-2">
			{tech_tags.map((tech) => <span>{tech}</span>)}
		</div>
	</div>
	<p>
		{description}
	</p>
	<a href={repoURL} target="_blank" class="font-light underline">
		{t('home.projects.card.link-in-overlay')}
	</a>
</div>
