---
import Button from '@components/ui/Button.astro'
import { t } from 'i18next'
---

<Button
	as="a"
	href={encodeURI("mailto:tomszwadesign@gmail.com?subject=Let's be friends!")}
	target="_blank"
	rel="noopener"
	aria-label="email"
>
	{t('site.contactButton')}
</Button>
